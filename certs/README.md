# CA Certificates

This directory simulates enterprise certificate management.

In real environments, this would include:
- Internal Root CA certificates
- Intermediate certificates
- Organization-specific trust bundles

These certificates are added to the container trust store during build.
